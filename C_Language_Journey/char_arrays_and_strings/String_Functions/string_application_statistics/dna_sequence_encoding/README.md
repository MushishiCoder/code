DNA序列的编码（游程编码/Run-Length Encoding简化版）
对于包含A、C、G、T四种字符的DNA序列，将其中连续k个相同的字符写成kX的形式（其中X代表字符）。如果k为1，则省略数字1，只保留字符本身。
遍历输入的DNA序列，用变量记录当前正在统计的字符(cur)及其重复出现的次数(count)。
遇到相同字符时计数器累加；遇到不同字符时，立即输出上一组的编码结果，并重置状态。
循环结束后，手动输出最后一组连续字符的编码。

输入：
AACAATGGGG
输出：
2AC2AT4G