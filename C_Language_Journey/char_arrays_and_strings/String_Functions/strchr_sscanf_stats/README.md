输入若干个不确定个数的整数和小数，分别统计其中整数的和与小数的和（题目中“小数”指带小数点的数，例如12是整数，12.00是小数）。
输入完毕后：
回车
Windows：按下键盘上的Ctrl+Z，然后按回车。
Mac/Linux：按下键盘上的Ctrl+D。

1.使用while(scanf("%s",str)!=EOF)循环读取不定量的数据，利用%s自动以空格/换行作为边界切分。
2.调用标准库函数strchr(str,'.')检查字符串中是否含有小数点来区分类型。
3.运用sscanf将字符串分别精准转化为int和double类型并进行累加。