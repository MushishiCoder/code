已知一个字符串表示一个实数（长度不超过100位），请编写程序输出其整数部分。输出要求不含前导0，如果整数部分全为0，则保留一个0。

读入字符串：安全接收输入的实数字符串。
去除前导0：移动字符指针跳过多余的0，但需注意若整数部分全为0（例如000.45），必须保留最后一个0。
查找小数点：通过strchr()函数在字符串中搜寻小数点.的位置并记录其地址。
截断小数部分：如果找到小数点，将其直接修改为字符串结束符'\0'，从而“物理抹杀”小数点及后面的所有小数部分。
输出结果：通过指针输出处理后的整数字符串。