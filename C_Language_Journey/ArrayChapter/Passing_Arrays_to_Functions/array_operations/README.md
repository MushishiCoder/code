编写一个C语言程序，实现利用数组作为函数参数的特性，将程序进行改写。具体要求如下
1.定义一个宏常量N表示数组的最大容量（如10或20）。
2.分别编写三个自定义函数来完成数组的：输入函数InputArr：接收数组及实际元素个数n，通过键盘读入n个整数存入数组。排序函数SortArr：接收数组及实际元素个数n，将该数组的n个元素按升序进行排序。输出函数PrnArr（或 DisArr）：接收数组及实际元素个数n，将排好序的数组元素输出到屏幕上。
3.在main函数中依次调用这三个函数，实现一维数组的输入、排序和输出。