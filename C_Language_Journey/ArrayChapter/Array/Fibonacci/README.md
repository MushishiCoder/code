输入一个正整数n（1<=n<=46），使用一维数组和递推方法计算并输出Fibonacci数列的前n项。数列前两项均为1，从第三项开始每一项等于前两项之和（f[i] = f[i-1]+ f[i-2]）。

数组的初始化技巧：部分初始化（如 {1,1}）可以简便地为前几项赋值，同时让剩余元素自动清零，避免垃圾值。
递推算法（Iteration）：与递归不同，递推通过循环“自底向上”利用已知旧数据推导新数据，逻辑清晰且性能高效。
输出排版控制：利用求余运算符%结合计数器，可以完美解决大量数据在一行内挤作一团的排版问题。