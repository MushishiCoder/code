定义一个联合类型，用其声明变量，为变量赋值后输出各成员的值

联合体的所有成员共用同一内存起始地址。当使用较小字节的成员（如4字节的float）进行赋值时，只会覆盖联合体前部的内存空间，后部未被覆盖的空间（如double的后半截）会保留之前的二进制残渣。

严禁在实际开发中依赖“未被完全覆盖而读取残余数据”的巧合行为。联合体的正确使用规范是在同一时刻仅能有效访问最后一次被赋值的成员，否则容易导致数据解析错乱或隐藏逻辑错误。