图书目录管理程序

安全字符串输入(s_gets)：
封装了标准库的fgets，能够有效防止缓冲区溢出。
自动抹除字符串末尾多余的换行符（\n）。
包含智能的缓冲区清理机制（利用while(getchar()!='\n')），彻底防止超长输入污染后续的数据读取。

结构体建模(struct book)：
将不同类型的数据（字符数组类型的书名、作者，以及浮点数类型的价格）打包封装在同一个数据容器中。